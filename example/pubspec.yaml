name: example
description: A sample command-line application.

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  enum_annotation: ^0.0.4

dev_dependencies:
  build_runner: ^2.4.4
  test: ^1.24.2
  enum_generator:
    path: ../