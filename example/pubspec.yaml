name: example
description: A sample command-line application.

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  enum_annotation: ^0.0.2

dev_dependencies:
  build_runner: ^2.3.2
  test: ^1.22.0
  enum_generator:
    path: ../