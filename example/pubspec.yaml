name: example
description: A sample command-line application.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  enum_annotation: ^0.0.4

dev_dependencies:
  build_runner: ^2.4.6
  test: ^1.24.6
  enum_generator:
    path: ../